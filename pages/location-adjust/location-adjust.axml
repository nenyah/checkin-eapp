<view>
  <map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="{{scale}}" controls="{{controls}}" onControlTap="controltap" markers="{{markers}}" onMarkerTap="markertap" onRegionChange="regionchange" onTap="tap"
  show-location style="width: 100%; height: 300px;">
  </map>
</view>